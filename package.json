{
  "name": "deadwire",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Deadwire — low-poly browser PvPvE extraction prototype (Core Run) + coded asset pipeline.",
  "scripts": {
    "dev": "node tools/serve.mjs",
    "game": "node tools/serve.mjs",
    "server": "node server/match-server.mjs",
    "start": "node server/match-server.mjs",
    "build:map": "tsc -p tsconfig.map.json",
    "check:map": "tsc -p tsconfig.map.json --noEmit",
    "qa:assets": "node tools/qa-assets.mjs",
    "qa:glb": "node game-asset-pipeline/scripts/qa-manifest.mjs",
    "stats": "node tools/model-stats.mjs"
  },
  "comment": "Runtime is zero-install (Three.js via CDN). 'three' below is dev-only, used by tools/model-stats.mjs for headless triangle/material budget checks.",
  "devDependencies": {
    "@types/three": "^0.160.0",
    "three": "^0.160.0",
    "typescript": "^5.8.3"
  }
}
